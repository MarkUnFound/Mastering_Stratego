%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\chapter{Results and Discussion}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\section{Normal DQN Performance Ceiling}

The multi-phase curriculum concluded with 37,213 episodes of Phase~4 League Training. The normal DQN agent used an LSTM-based history encoder and trained against random, greedy heuristic, league, and self-play opponents. Table~\ref{tab:vanilla_performance} reports the results.

\begin{table}[htbp]
    \centering
    \caption{Normal DQN performance after 37,213 episodes of Phase~4 League Training.}
    \label{tab:vanilla_performance}
    \begin{tabular}{|l|c|c|c|c|}
        \hline
        \textbf{Opponent Type} & \textbf{Episodes} & \textbf{Wins} & \textbf{Win Rate} & \textbf{Draw Rate} \\
        \hline
        Greedy Heuristic & 9,319 & 1,418 & 15.2\% & 41.3\% \\
        Self-Play        & 9,565 & 2,370 & 24.8\% & 36.8\% \\
        Random           & 9,294 & 2,260 & 24.3\% & 37.7\% \\
        League           & 9,035 & 2,231 & 24.7\% & 38.2\% \\
        \hline
        \textbf{Aggregate} & \textbf{37,213} & \textbf{8,279} & \textbf{22.2\%} & \textbf{38.5\%} \\
        \hline
    \end{tabular}
\end{table}

Six empirical findings emerge from the training data. Each points to a distinct architectural limitation.

\paragraph{Finding 1: Win Rate Plateau.}
Agent~1's cumulative win rate settled near 22\% within 2,000 episodes. It never improved over the next 35,000. The 100-episode rolling average swung between 15\% and 30\% with no upward trend. The policy stalled.

\paragraph{Finding 2: Loss--Performance Decoupling.}
The TD error fell monotonically to roughly 0.001. The win rate did not follow. The network learned to predict the value of passive, draw-seeking play accurately. It did not discover better strategies.

\paragraph{Finding 3: Q-Value Overestimation.}
Average Q-values climbed from 0.2 to 1.2. Win rates did not move. Rising value estimates without corresponding performance gains point to systematic overestimation bias.

\paragraph{Finding 4: LSTM Prediction Accuracy Ceiling.}
The LSTM piece-identity module plateaued at 18--22\% accuracy, a 2.2--2.6$\times$ gain over the 8.3\% random baseline but far short of reliable tactical inference. The curve flattened after episode 5,000 and never recovered.

\paragraph{Finding 5: Opponent-Specific Performance Asymmetry.}
Performance varied sharply by opponent. Win rate against the greedy heuristic was 15.2\%; in self-play it reached 24.8\%. The policy fit its own behavioral distribution and could not generalize.

\paragraph{Finding 6: Draw Dominance.}
Draws accounted for 38.5\% of outcomes, far above the 22.2\% win rate. The agent shuffled pieces passively and avoided uncertain engagements. It minimized short-term losses at the expense of long-term winning probability.

\begin{figure}[htbp]
    \centering
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth,height=0.25\textheight,keepaspectratio]{figures/Results/training_progress_episode_37000.png}
        \caption{Reward convergence and win rate trajectory.}
    \end{subfigure}
    \hfill
    \begin{subfigure}[b]{0.48\textwidth}
        \centering
        \includegraphics[width=\textwidth,height=0.25\textheight,keepaspectratio]{figures/Results/additional_metrics_episode_37000.png}
        \caption{Q-value, episode length, and exploration metrics.}
    \end{subfigure}
    \caption{Training diagnostics at episode 37,000.}
    \label{fig:training_diagnostics_37k}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.65\textwidth,height=0.3\textheight,keepaspectratio]{figures/Results/aaren_progress_episode_37000.png}
    \caption{AAREN/LSTM training metrics at episode 37,000.}
    \label{fig:aaren_diagnostics_37k}
\end{figure}


\section{Behavioral Patterns from Training Logs}

The 37,213-episode logs reveal four recurring patterns behind the normal DQN's limitations.

\subsection{Passive Shuffling Convergence}

The agent grew increasingly conservative. Average episode length rose throughout training as it avoided combat and cycled pieces between adjacent squares. Scalar value collapse (Finding~2) explains why: when the Q-network cannot separate high-variance attacks from low-variance shuffles, the policy drifts toward the lowest-variance action class.

\subsection{Opponent-Specific Adaptation Failure}

Table~\ref{tab:vanilla_performance} shows the agent overfitting to its own play style. The greedy heuristic attacks the nearest piece regardless of rank uncertainty, exploiting passive shuffling. In self-play, both agents adopt similar caution, inflating the apparent win rate through symmetric outcomes. Fixed $\epsilon$-greedy decay provides no mechanism for adapting exploration to novel opponent behavior.

\subsection{Loss-Reward Divergence}

TD loss dropped from 0.05 to 0.001 by episode 37,000. Average episode reward stayed flat near 0.08. The network learned to predict outcomes of its existing passive play with high precision. It did not search for strategies with higher expected returns.

\subsection{LSTM Accuracy as an Information Barrier}

Piece prediction accuracy hit 20\% after episode 5,000 and stayed there for 32,000 more episodes. One correct inference in five is not enough to evaluate combat outcomes reliably, so the agent avoids combat altogether. Continued gradient updates could not push past this ceiling. The 64-dimensional hidden state simply lacks the capacity for accurate belief tracking over 200 to 500 move sequences.


\section{Draw Dominance and Learning Signal Quality}

Draws constituted 38.5\% of all games. This rate matters not just as a performance number but as a drag on learning itself.

The reward structure assigns $+1.0$ for flag captures, $+0.5$ for depletion wins, $-1.0$ for losses, and $-0.3$ for draws. A loss sends a gradient signal 3.3 times stronger than a draw. Losses clearly mark failed strategies. Draws are near-zero signals that carry almost no information about whether the preceding actions were good or bad.

The preference for draws is a symptom of scalar value collapse (Finding~2). A scalar Q-value cannot represent multi-modal outcome distributions. A move with 40\% win probability and 60\% loss probability (expected value $\approx -0.2$) looks worse than a certain draw ($-0.3$) to a mean-based estimator. The draw-producing move gets the higher Q-value even though it is strategically inferior.

Draw dominance is therefore not just a failure to win. It is a self-reinforcing trap: the agent suppresses the informative loss signals that would push it toward better strategies. A distributional representation like C51 would preserve outcome modality and let the policy weigh safe draws against risky but decisive actions.


\section{Architectural Comparison: Unified versus Dual-Model Pipeline}

An earlier version used two separate networks: a C51-based SetupAgent for piece placement and a DQN for movement. That dual-model design was abandoned. The current unified pipeline uses a single DQN for all movement and a randomized heuristic for placement. Table~\ref{tab:architecture_comparison} compares both.

\begin{table}[htbp]
    \centering
    \caption{Dual-model versus unified architectural pipelines.}
    \label{tab:architecture_comparison}
    \begin{tabular}{|p{3cm}|p{5.5cm}|p{5.5cm}|}
        \hline
        \textbf{Aspect} & \textbf{Dual-Model (Setup + Attack)} & \textbf{Unified DQN} \\
        \hline
        Architecture & Separate C51-based SetupAgent for placement, separate DQN for attack & Single DQN for movement; heuristic setup with randomized flag \\
        \hline
        Setup Latency & Approximately 2 seconds per episode & Under 100 milliseconds per episode \\
        \hline
        Training Overhead & Doubled compute requirement (setup and attack training phases) & Halved total training compute \\
        \hline
        Exploitability & Deterministic setup patterns learned by opponents & Randomized placement resists opponent exploitation \\
        \hline
        Draw Pattern & Draws due to gradient interference between competing objectives & Draws due to risk-averse policy under uncertainty \\
        \hline
    \end{tabular}
\end{table}

Two lessons emerged from this transition. The SetupAgent converged on deterministic placements, creating an exploitable pattern. Opponents that trained against those fixed flag positions and bomb layouts learned to exploit them. A randomized heuristic eliminated this vulnerability and cut setup latency by 20$\times$.

The dual-model design also suffered from gradient interference. Setup gradients rewarded defensive formations; attack gradients rewarded aggressive piece use. Optimal defense often places pieces in positions bad for attack. Removing the setup phase from the learning loop resolved this conflict.

Both architectures converged on draw-heavy outcomes. In the dual case, gradient interference between objectives prevented coherent learning. In the unified case, scalar value collapse and poor exploration drove the agent into passive shuffling. The draw problem is architectural, not an engineering accident.


\section{Architectural Bottleneck Analysis}

The findings above are not artifacts of insufficient training or bad hyperparameters. Each one follows from a known structural property of the normal DQN.

\subsection{Q-Value Overestimation}

Finding~3 reflects a known property of single-estimator Q-learning. The network selects and evaluates actions with the same weights:

\begin{equation}
y_t = r_t + \gamma \max_{a'} Q_\theta(s_{t+1}, a')
\end{equation}

Noisy estimates plus a max operator produce a positive bias \cite{RainbowJMLR2021}:

\begin{equation}
\mathbb{E}\left[\max_{a} (Q^*(s, a) + \epsilon_a)\right] \geq \max_{a} Q^*(s, a)
\end{equation}

The bias grows with noise variance and action-space size $|\mathcal{A}|$. Stratego positions can have over 200 legal moves, amplifying the effect. Double DQN splits selection from evaluation across two estimators, provably reducing this bias \cite{RainbowJMLR2021}.

\subsection{Scalar Value Collapse}

Finding~2 (loss convergence without performance gain) follows from distributional RL theory. The standard DQN learns a scalar $\mathbb{E}[Z(s,a)]$ and minimizes:

\begin{equation}
\mathcal{L} = \mathbb{E}\left[\left(r + \gamma \max_{a'} Q(s', a') - Q(s, a)\right)^2\right]
\end{equation}

In Stratego the return distribution $Z(s,a)$ is multi-modal \cite{RainbowJMLR2021}: wins near $+1$, losses near $-1$, exchanges in $[-0.5, +0.5]$, draws near $0$. A scalar mean collapses all modes into one number that may not match any real outcome. The loss converges when this mean is estimated correctly, even if the policy cannot tell aggressive high-variance moves from passive low-variance ones.

C51 models the full distribution over 51 atoms. Its projected Bellman operator contracts in Cram\'{e}r distance, giving formal convergence guarantees \cite{RainbowJMLR2021}.

\subsection{LSTM Information Bottleneck}

Finding~4 follows from sequential compression. The LSTM encodes observations $o_1, \ldots, o_T$ through a 64-dimensional hidden state:

\begin{equation}
h_t = \text{LSTM}(o_t, h_{t-1})
\end{equation}

A Stratego game runs 200 to 500 moves. The hidden state must track belief estimates for 40 opponent pieces across 12 types. A 64-dimensional vector can hold enough information in theory, but every successive nonlinear update risks losing early observations. Information from move $k$ must survive $T - k$ transformations to reach the final representation. Gating helps, but retention still degrades with sequence length \cite{Aaren}.

AAREN sidesteps this bottleneck. It computes weighted aggregations directly over the full history with $O(1)$ per-timestep complexity:

\begin{equation}
h_t = \frac{a_t}{c_t} = \frac{\sum_{k=1}^{t} v_k \exp(s_k - m_t)}{\sum_{k=1}^{t} \exp(s_k - m_t)}
\end{equation}

No sequential compression, no information decay. The network can attend to any prior timestep directly.

\subsection{Exploration Collapse}

The Noisy Net sigma stayed at zero throughout training (Figure~\ref{fig:training_diagnostics_37k}). The $\epsilon$-greedy schedule decayed from 1.0 to 0.01 and then stopped adapting. After that, the agent explored uniformly at 1\% regardless of game state.

Stratego has over $10^{535}$ states \cite{perolat2022stratego}. Uniform random exploration at 1\% will almost never stumble on improved strategies. Noisy Networks \cite{RainbowJMLR2021} make exploration learnable: per-weight noise parameters $\sigma_w$ let the agent concentrate uncertainty where the value function is least reliable.

\subsection{Sparse Reward Propagation}

The normal DQN cannot develop long-horizon strategic behavior (Findings~1, 5, 6), a limitation compounded by single-step TD learning. Terminal reward information at step $T$ propagates backward through the Bellman backup at a rate set by the discount factor $\gamma$:

\begin{equation}
Q(s_k, a_k) \leftarrow Q(s_k, a_k) + \alpha \left( r_k + \gamma Q(s_{k+1}, a_{k+1}) - Q(s_k, a_k) \right)
\end{equation}

For a decisive game outcome occurring at step $T$ in a game of length 300, a mid-game decision at step 100 receives an effective signal of $\gamma^{200}$. With $\gamma = 0.99$, this yields $0.99^{200} \approx 0.134$, meaning that only 13.4\% of the terminal reward signal reaches mid-game decisions through single-step bootstrapping. Multi-step returns ($n$-step TD) \cite{RainbowJMLR2021} propagate terminal signals $n$ times faster by directly incorporating rewards over $n$ consecutive steps:

\begin{equation}
y_t^{(n)} = \sum_{k=0}^{n-1} \gamma^k r_{t+k} + \gamma^n Q(s_{t+n}, a_{t+n})
\end{equation}

This reduces the effective bootstrap depth and accelerates the propagation of sparse terminal rewards to earlier decision points.


\section{Evidence for Architectural Necessity}

Each component in the thesis title maps to a specific failure mode observed in the normal DQN baseline. Table~\ref{tab:title_evidence} summarizes this mapping.

\begin{table}[htbp]
    \centering
    \caption{MARQ title components mapped to normal DQN failure modes.}
    \label{tab:title_evidence}
    \begin{tabular}{|p{2.8cm}|p{5cm}|p{6cm}|}
        \hline
        \textbf{Title Component} & \textbf{Observed Failure Mode} & \textbf{Evidence from Training Data} \\
        \hline
        Multi-Agent & Symmetric MARL Nash lock-in & P1 and P2 win rates converged to near-identical 25.1\% and 24.9\% respectively over the first 10,000 episodes. Introduction of lagged league opponents broke this symmetry, confirming that the multi-agent training architecture is essential for progress beyond equilibrium. \\
        \hline
        Rainbow DQN & Q-value overestimation, scalar value collapse, exploration death, sparse reward propagation & Findings 2 through 5 each map to a specific Rainbow component: Double DQN addresses overestimation (F3), C51 addresses scalar collapse (F2), Noisy Nets address exploration collapse (F4), and $n$-step returns address reward propagation (F5). \\
        \hline
        AAREN & Sequential information bottleneck & LSTM accuracy ceiling at 18 to 22\% (F4) persisted for 32,000 episodes despite continued gradient updates, indicating that the 64-dimensional recurrent hidden state lacks the representational capacity for long-horizon piece identity inference. AAREN's direct attention over the full observation history addresses this structural limitation. \\
        \hline
        Imperfect Information & Risk-averse draw-seeking policy & The 38.5\% draw rate (F6) is a direct consequence of hidden piece identities. The agent learned that avoiding combat with unknown pieces minimizes short-term loss, producing passive shuffling that sacrifices long-term winning probability. \\
        \hline
    \end{tabular}
\end{table}

These findings converge on one conclusion: the full MARQ architecture is justified. Each component targets a distinct, verified bottleneck. Draw dominance (F6) is a compound effect, confirming that no single fix is sufficient.


\section{Supporting Evidence from Related Work}

These limitations are consistent with findings elsewhere in deep RL for imperfect information games.

P\'{e}rolat et al. (2022) solved full-size Stratego with DeepNash \cite{perolat2022stratego} using game-theoretic regularization (R-NaD), not Q-value maximization. DeepNash shows Stratego is solvable by deep RL, but only when the architecture explicitly handles non-stationarity and strategic diversity. The Ataraxos system \cite{Ataraxos2025} extended this to no-press Diplomacy with KL-divergence stabilization, a mechanism similar to MARQ's dynamic damping but absent from the normal DQN baseline.

Hessel et al. (2021) showed in the Rainbow DQN study that Double DQN, PER, Dueling Networks, Noisy Nets, C51, and multi-step returns each contribute additive gains \cite{RainbowJMLR2021}. Removing any single component degraded performance. No one fix is enough.


\section{Implications for MARQ}

The performance ceiling stems from identifiable architectural properties of the normal DQN, not from insufficient training or bad hyperparameters. Each limitation maps to a specific MARQ component, as shown in Table~\ref{tab:limitation_mapping}.

\begin{table}[htbp]
    \centering
    \caption{Observed limitations mapped to theoretical causes and MARQ components.}
    \label{tab:limitation_mapping}
    \begin{tabular}{|p{3cm}|p{4cm}|p{3.5cm}|p{2cm}|}
        \hline
        \textbf{Finding} & \textbf{Theoretical Cause} & \textbf{MARQ Component} & \textbf{Status} \\
        \hline
        Q-Value Overestimation & Maximization Bias & Double DQN / Dueling & Proposed \\
        \hline
        Loss--Perf. Decoupling & Scalar Value Collapse & C51 Distributional RL & Proposed \\
        \hline
        LSTM Accuracy Ceiling & Sequential Bottleneck & AAREN Attention & Proposed \\
        \hline
        Exploration Collapse & Fixed $\epsilon$-greedy & Noisy Networks & Proposed \\
        \hline
        Reward Propagation & 1-step Bootstrapping & $n$-step Returns & Proposed \\
        \hline
        Draw Dominance & Compound Effect & Full MARQ Pipeline & Proposed \\
        \hline
    \end{tabular}
\end{table}

The analysis provides a principled basis for the proposed enhancements. Empirical validation of the full MARQ pipeline remains future work. The normal DQN baseline in this chapter serves as a controlled reference point for systematic ablation of each MARQ component.
